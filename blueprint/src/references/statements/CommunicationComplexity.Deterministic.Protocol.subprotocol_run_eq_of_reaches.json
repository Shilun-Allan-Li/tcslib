{
 "lean_name": "CommunicationComplexity.Deterministic.Protocol.subprotocol_run_eq_of_reaches",
 "key": "f24f4c6bbf53eeec",
 "kind": "lemma",
 "title": "Subprotocol and parent agree on inputs reaching it",
 "statement": "Let $p$ and $s$ be deterministic two-party communication protocols with Alice's inputs drawn from $X$, Bob's inputs from $Y$, and outputs in $\\alpha$, and suppose $s$ is a subprotocol of $p$, that is, a rooted subtree of $p$. If an input pair $(x, y)$ reaches $s$ — meaning $x$ is consistent with every one of Alice's branch choices and $y$ with every one of Bob's branch choices along the path from the root of $p$ down to the root of $s$ — then evaluating $p$ on $(x, y)$ returns the same output as evaluating $s$ on $(x, y)$.",
 "hypotheses_covered": [
  "$s$ is a subprotocol of $p$",
  "the input pair $(x,y)$ reaches the subprotocol $s$"
 ],
 "confidence": "high",
 "caveat": "",
 "rubric": [],
 "replaced": "If \\texttt{hsp} witnesses that $s$ is a subprotocol of $p$ and $(x, y)$ reaches $s$, then\n$p.\\texttt{run}\\;x\\;y = s.\\texttt{run}\\;x\\;y$."
}
