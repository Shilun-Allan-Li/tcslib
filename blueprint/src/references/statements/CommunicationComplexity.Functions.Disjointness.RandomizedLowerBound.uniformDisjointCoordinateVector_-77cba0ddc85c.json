{
 "lean_name": "CommunicationComplexity.Functions.Disjointness.RandomizedLowerBound.uniformDisjointCoordinateVector_indep_coordinateXBit_coordinateYBefore_condBits",
 "key": "5f060253ebe9bb82",
 "kind": "theorem",
 "title": "Conditional independence of Alice's coordinate bit and Bob's earlier bits",
 "statement": "Fix a positive integer $n$, a coordinate $i \\in \\{0,1,\\dots,n-1\\}$, and a bit vector $b \\colon \\{0,\\dots,n-1\\} \\to \\{0,1\\}$. Draw a coordinate vector uniformly from the $n$-fold product of the three-element set whose members mark a coordinate as belonging to neither party, to Alice only, or to Bob only; write $X_k = 1$ exactly when coordinate $k$ belongs to Alice only and $Y_k = 1$ exactly when it belongs to Bob only. Condition on the event that Alice's conditioning bit vector — whose $k$-th entry is $X_k$ when $k < i$ and $Y_k$ when $k \\ge i$ — equals $b$. Under this conditional law, Alice's bit $X_i$ at coordinate $i$ and the vector of Bob's earlier bits, equal to $Y_j$ for each $j < i$ and to $0$ at every $j \\ge i$, are independent random variables.",
 "hypotheses_covered": [
  "positive number of coordinates $n$",
  "fixed coordinate $i \\in \\{0,\\dots,n-1\\}$",
  "fixed conditioning bit vector $b$",
  "uniform law on three-state disjoint-coordinate vectors",
  "conditioning on Alice's conditioning bit vector equalling $b$"
 ],
 "confidence": "high",
 "caveat": "",
 "rubric": [],
 "replaced": "After fixing Alice's one-bit-per-coordinate conditioning, the current Alice bit is independent of Bob's earlier bits under the product-uniform disjoint-coordinate law."
}
