{
 "lean_name": "CommunicationComplexity.Functions.Disjointness.RandomizedLowerBound.volume_zVariable_dualProtocol_dualZValue_inter_specialX",
 "key": "4ced183c8d908c64",
 "kind": "theorem",
 "title": "Mass of dual and original special-bit Z-fibers",
 "statement": "Let $n$ be a positive integer and let $p$ be a deterministic communication protocol for the disjointness inputs of length $n$. For a sample $\\omega$ of the hard distribution, let $Z_p(\\omega)$ denote the statistic recording the transcript of $p$ on the input pair generated by $\\omega$, together with the special coordinate $T$, Alice's input bits strictly before $T$, and Bob's input bits strictly after $T$ (each padded with $\\mathrm{false}$ off its range); write $Z_p^{-1}(z)$ for the set of samples on which this statistic takes a fixed achievable value $z$. Let $p^{*}$ be the dual protocol, obtained by exchanging the roles of Alice and Bob and reversing both coordinate sets, and let $z^{*}$ be the correspondingly dualized value of the statistic for $p^{*}$. Then for each bit $b$, the set of samples with $Z_{p^{*}}(\\omega) = z^{*}$ whose special coordinate carries Alice's bit equal to $b$ has the same mass, under the ambient measure on the finite space of hard samples, as the set of samples with $Z_p(\\omega) = z$ whose special coordinate carries Bob's bit equal to $b$.",
 "hypotheses_covered": [
  "n is a positive integer (length of the disjointness inputs)",
  "p is a deterministic protocol for the length-n disjointness inputs",
  "z is an achievable value of the Z-statistic for p",
  "b is a bit"
 ],
 "confidence": "high",
 "caveat": "",
 "rubric": [],
 "replaced": "The ambient hard-sample measure gives corresponding original and dual one-bit $Z$-fiber events the same mass."
}
