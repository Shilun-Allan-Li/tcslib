{
 "lean_name": "SwitchingLemma2.encode_go_acc",
 "key": "f81247573879465e",
 "kind": "lemma",
 "title": "Accumulator decomposition for the encoder loop",
 "statement": "Fix a DNF formula $f$ on $n$ variables — a list of terms, each a conjunction of literals — together with a width parameter $w \\in \\bbn$, a fuel bound $\\mathrm{fuel} \\in \\bbn$, a path $P$ recording the variable–value pairs already committed (a list of elements of $\\mathrm{Fin}\\,n \\times \\mathrm{Bool}$), two restrictions $\\rho_0, \\sigma : \\mathrm{Fin}\\,n \\to \\mathrm{Option}\\,\\mathrm{Bool}$, and an accumulator $A$, a list of index–value pairs in $\\bbn \\times \\mathrm{Bool}$. The tail-recursive helper of Razborov's encoding procedure, run from the initial accumulator $A$ on these data, produces the same output as the corresponding run started from the empty accumulator, with the entries of $A$ carried through unchanged; that is, the accumulator contributes to the result only additively and does not affect the encoding computed by the procedure itself.",
 "hypotheses_covered": [
  "a DNF formula f on n variables",
  "a width parameter w",
  "a fuel bound",
  "a path of variable-value pairs",
  "a restriction ρ₀",
  "a restriction σ",
  "an initial accumulator of index-value pairs"
 ],
 "confidence": "low",
 "caveat": "The formal statement's right-hand side is given as `sorry`, so the precise form of the equality — exactly how the empty-accumulator run is combined with the supplied accumulator — cannot be verified against the ground truth. The rendering above reflects the accumulator-decomposition shape suggested by the existing informal statement, not a fully determined conclusion. In addition, the encoding helper `razborovEncode.go` is not among the supplied definitions, so its exact operation is inferred from its signature rather than known.",
 "rubric": [],
 "replaced": "The encoder loop \\texttt{SwitchingLemma2.razborovEncode}'s \\texttt{go} run with an\naccumulator decomposes into a run on the empty accumulator: its result can be expressed\nin terms of the corresponding empty-accumulator computation together with the supplied\naccumulator."
}
