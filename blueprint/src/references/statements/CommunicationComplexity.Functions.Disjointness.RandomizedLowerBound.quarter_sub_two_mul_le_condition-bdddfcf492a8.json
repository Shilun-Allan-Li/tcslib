{
 "lean_name": "CommunicationComplexity.Functions.Disjointness.RandomizedLowerBound.quarter_sub_two_mul_le_conditionalSpecialPairLaw_singleton",
 "key": "d3bff5efa0a393da",
 "kind": "theorem",
 "title": "Uniform lower bound on singleton masses of a good conditional law",
 "statement": "Fix a deterministic disjointness protocol $p$ on inputs of length $n$, a real number $\\gamma$, and a value $z$ of the transcript-statistic. Suppose $z$ is *good* for $\\gamma$, meaning that the conditional special-pair law given $z$ — the distribution of the special-coordinate bit-pair $(x_T, y_T)$ conditioned on the value $z$ — lies within total variation distance $2\\gamma$ of the uniform law on bit-pairs. Then for every pair $b \\in \\{0,1\\} \\times \\{0,1\\}$, this conditional special-pair law assigns to the singleton $\\{b\\}$ a mass of at least $\\tfrac14 - 2\\gamma$.",
 "hypotheses_covered": [
  "p is a deterministic disjointness protocol on inputs of length n",
  "gamma is a real number",
  "z is a value of the transcript-statistic ZType",
  "z is good for gamma: total variation distance between the conditional special-pair law and the uniform bit-pair law is at most 2*gamma",
  "b is a bit-pair"
 ],
 "confidence": "high",
 "caveat": "",
 "rubric": [],
 "replaced": "A good $z$ gives a lower bound on each singleton mass of the conditional special-pair law."
}
